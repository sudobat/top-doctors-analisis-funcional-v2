# Timing and token notes

Accurate token usage and runtime metrics were not captured for iteration-1.

The benchmark run directories include placeholder `timing.json` files with zero values:
- `total_tokens`: 0
- `duration_ms`: 0
- `total_duration_seconds`: 0.0

Reason: timing was not captured from background subagent notifications for this iteration.
